  
version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.8.1
    steps:
      - checkout
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run: pip install pystrich
      - run:
          name: Hello World x times
          command: 
            .
            python app.py


